# SPFx skeleton — DCE Hub Home web part

This is the **portability proof** for the Tier-B HTML mockup. Every section in
`../index.html` has a 1:1 React component here. The mockup's audience-targeting
`data-audience` attribute becomes `<AudienceGate groups={[...]}>`.

This is a skeleton, not a buildable solution. To turn it into a real SPFx
project:

1. Scaffold a new SPFx 1.22+ web part with `yo @microsoft/sharepoint`. Pick
   "minimal" template, React framework, TypeScript.
2. Replace the generated web part class with `DceHubHomeWebPart.ts`.
3. Copy `theme/` and `audience/` and `components/` into `src/`.
4. Add the Fluent UI v9 dependency: `npm i @fluentui/react-components`.
5. Add `@microsoft/sp-http` (already present in SPFx 1.22 scaffolds).
6. Build with the **Convention-Page-Build/spfx** pattern — Heft, not gulp.

## Reuse map (rubric criterion 1)

| Source | Reused for |
|---|---|
| `/Users/tygranlund/dev/01-htt-brands/Convention-Page-Build/spfx/package.json` | Pin Node 22.14+, React 17.0.1, TypeScript 5.3.3, PnP.js, Fluent UI |
| `/Users/tygranlund/dev/01-htt-brands/Convention-Page-Build/spfx/deploy-spfx.ps1` | Adapt as `ci-cd/scripts/deploy-prod.ps1` |
| `/Users/tygranlund/dev/01-htt-brands/Convention-Page-Build/spfx/src/tokens/convention-tokens.ts` | Pattern source for `theme/dce-tokens.ts` |
| `/Users/tygranlund/dev/01-htt-brands/Convention-Page-Build/spfx/src/webparts/conventionHero/ConventionHero.tsx` | Structural template for `components/DceHero.tsx` |
| `docs/sharepoint-pnp-spec/reference/dce-tokens.json` | Source of truth for all color/typography/spacing |

## Version pins (ADR-006 final, 2026-05-16, evidence-verified)

```json
"engines": { "node": ">=22.14.0 <23.0.0" },
"@microsoft/sp-*":           "1.22.2",   // MS Learn compat 2026-03-11; not 1.22.0 as Convention-Page-Build pinned
"@fluentui/react":           "8.121.0",  // v8 — NOT v9 per the locked ADR
"@pnp/sp":                   "4.9.0",
"@pnp/spfx-controls-react":  "3.20.0",
"react":                     "17.0.1",   // EXACT pin — peer dep is >=16.13.1 <18.0.0; React 18 unsupported
"typescript":                "5.3.3"
```

**Heft CSS/SCSS bugs to avoid** (5 confirmed in SPFx 1.22+):
- SASS degradation (#10466) — use simple nesting only
- CSS source maps broken in dev (#10831)
- .scss/.css typed as CSS modules but processed as global (#10832)
- JavaScript heap OOM (#10796)
- Hash appending on sp-dev-fx-controls-react (#10603)

## Section-to-component table

| Mockup section | `data-component` | React component | Audience |
|---|---|---|---|
| Hero | `dce-hero` | `<DceHero />` | all |
| Quick links | `dce-quicklink-strip` | `<DceQuicklinks />` (todo) | all |
| KPI tiles | `dce-kpi-tile` | `<AudienceGate groups={[Owners,Mgrs,Lead,Adm]}><DceKpiRow /></AudienceGate>` | R1/R2/R3/R4 |
| News feed | `dce-news-feed` | `<DceNewsFeed />` (todo) | all |
| Owner spotlight | `dce-people-spotlight` | `<DcePeopleSpotlight />` (todo) | all |
| Events | `dce-event-list` | `<DceEventList />` (todo) | all |
| Ops alerts | `dce-news-feed-ops` | `<AudienceGate groups={[Mgrs,Staff]}><DceNewsFeed kind="ops" /></AudienceGate>` | R4/R5 |
| Brand resources | `dce-card-grid` | `<DceCardGrid />` (todo) | all |
| Footer | `dce-brand-footer` | Application Customizer extension (separate) | all |

## What's wired up vs. stubbed

- ✅ **DceHero** — full implementation, structurally matches the mockup.
- ✅ **DceKpiTile** — full implementation.
- ✅ **AudienceGate + useAudience hook** — wired to Graph `/me/transitiveMemberOf`.
- ✅ **Fluent UI v9 theme** generated from DCE tokens.
- ⏳ **DceQuicklinks, DceNewsFeed, DcePeopleSpotlight, DceEventList, DceCardGrid** — left
  as todo to keep the skeleton readable. Same import + style pattern as the two
  implemented components.
- ⏳ **JSON manifest, property pane definition** — generated by SPFx
  scaffolding; not included here.

## Property pane (future)

Each section's audience and content list should be exposed via the
property pane so editors can adjust without code changes. Pattern:

```ts
PropertyPaneTextField('heroTitle', { label: 'Hero headline' }),
PropertyPaneTextField('audienceGroupsKpi', { label: 'KPI tile audience (semicolon-separated)' }),
```

## Tests

Per 06-tooling-pnp.md and 08-quality-gates.md:

- Unit tests with Jest (SPFx default).
- Component tests with React Testing Library.
- E2E with Playwright against the dev site.
- a11y with axe-core in CI.

## Anti-patterns avoided

1. **No inline hex.** All colors imported from `dce-tokens.ts`.
2. **No hardcoded audience names.** Group names are string constants in
   the web part class properties — editors swap them via the property pane.
3. **No "show this to user X" — always groups.** Matches the 02-identity-audience.md philosophy.
4. **No permission breaks performed by web part code.** Permissions remain
   a PnP/PowerShell concern; the web part only reads membership.
